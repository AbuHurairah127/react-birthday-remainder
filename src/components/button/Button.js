import React from "react";
import "./Button.css";
const Button = (props) => {
  return (
    <div>
      <button className="button-86">{props.label}</button>
    </div>
  );
};

export default Button;
